# Environment variables for local development and docker-compose

# Database
DB_PASSWORD=password
DB_NAME=drinking
DB_PORT=5432

# PgAdmin (optional - used when running with compose profile "admin")
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin
PGADMIN_PORT=8080

# App
NODE_ENV=development
AUTH_URL=http://localhost:3000
AUTH_SECRET=UjNzOkwQ8eBspnsc1FgiCKyocv0PhcriaZ8d5ttFBVY=
AUTH_DISCORD_ID=abcd
AUTH_DISCORD_SECRET=abdc

# Prisma/Database URL for local host usage (outside containers)
# When running inside docker, compose sets this in the service to point at the
# postgres container host. For local runs (pnpm dev/build), we point to localhost.
DATABASE_URL=postgresql://postgres:password@localhost:5432/drinking
